# Postponed Work

Work in this section is intentionally outside Looper's active direction. It is
retained so decisions, proof, and dependency context are not lost.

## System Mapper

System Mapper is deprecated for now. Its historical interactive-renderer bead
set and checkpoint are stored in [`system-mapper/`](./system-mapper/).

## System Understanding Artifacts

The replacement artifact-pipeline plan is paused after `sua-004`. Its PRD,
35-bead execution plan, checkpoint, issue packets, and proof outputs are stored
in [`system-understanding-artifacts/`](./system-understanding-artifacts/).

Resume either body of work only after an explicit product-direction decision.
